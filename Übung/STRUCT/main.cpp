#include <iostream>
#include <string.h>
#include <vector>

using namespace std;


int main()
{
    struct person {
        char vorname[20] = "";
        char nachname[20] = "";
        int id;
    };

    int main() {

        char vn[] = "Eduard";
        person edi;
        edi
        strcpy(edi.vorname, vn);
        char nn[] = "Kampf";
        strcpy(edi.nachname, nn);
        edi.id = 1;

    }
}
